<div class="form-floating mb-3">
    <input type="{{ $type }}" class="form-control" name="{{ $name }}" id="{{ $name }}" placeholder=" " value="{{ $value }}">
    <label for="{{ $name }}">{{ $placeholder }}</label>
</div>