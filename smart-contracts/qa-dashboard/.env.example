# Copy to .env.local for BSC Testnet QA (never commit real keys)

# BSC Testnet
VITE_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/
VITE_CHAIN_ID=97

# Testnet deployer private key (root wallet). Testnet QA only — never mainnet.
VITE_DEPLOYER_PK=

# Local Hardhat (defaults if .env.local omitted):
# VITE_RPC_URL=http://127.0.0.1:8545
# VITE_CHAIN_ID=31337
# VITE_DEPLOYER_PK= (uses built-in Hardhat account #0)
