# QA Dashboard — BSC Testnet
# Copy to .env  (never commit .env)

VITE_RPC_URL=https://bsc-testnet-rpc.publicnode.com
VITE_CHAIN_ID=97

# Same private key used for npm run deploy:bsc-testnet (root / MockBTCB owner)
# Format: 0x + 64 hex chars — do NOT paste into chat
VITE_DEPLOYER_PK=
